策略模式的意图是将可互换的方法封装在各自独立的类中
并且让每个方法都实现一个公共的操作

策略模式的重心不是如何实现算法，
而是如何组织、调用这些算法，从而让程序结构更灵活，具有更好的维护性和扩展性。

策略算法是相同行为的不同实现。

Context封装角色
	构造传入策略接口 根据传入不同的策略实例 实现不同的策略
Strategy抽象策略角色
	定义通用方法
ConcreteStrategy具体策略角色
	对策略通用方法具体实现

算法自由切换
避免使用多重条件判断
扩展性良好	